.DS_Store
.worktrees/

# install.sh backups
*.bak.*

# cmux-skills local install (installed via npx skills add manaflow-ai/cmux-skills)
.agents/
skills-lock.json

# secrets — never commit
.env
.env.*
!.env.example
!.env.sample
*.pem
*.key
*.p12
